pipeline {
    agent {
        label 'QA'
    }
    stages {
        stage('for main branch') {
        when {
            branch 'main'
        }
        steps {
            echo 'main branch is good'
        }
        }
        stage('for stage branch') {
        when {
            branch 'dev'
        }
        steps {
            echo 'dev branch is not good'
        }
        }
        stage('for pull request') {
        when {
            branch 'PR-*'
        }
        steps {
            echo 'pull request is notvery not good'
        }
        }
    }
}